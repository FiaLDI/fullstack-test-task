import { formatDate } from "./format-date";
import { formatSize } from "./format-size";
import { getLevelVariant } from "./get-level-variant";
import { getProcessingVariant } from "./get-progressing-variant";

export const Helpers = {
    formatDate,
    formatSize,
    getLevelVariant,
    getProcessingVariant,
}
